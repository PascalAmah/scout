[project]
name = "scout-api"
version = "0.1.0"
description = "Scout API — AI-native startup job search workspace"
readme = "README.md"
authors = [
    { name = "PascalAmah", email = "pascal.amah10@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "sqlalchemy>=2.0.30",
    "alembic>=1.13.0",
    "psycopg[binary]>=3.2.0",
    "pydantic>=2.7.0",
    "pydantic-settings>=2.3.0",
    "pyjwt>=2.8.0",
    "argon2-cffi>=23.1.0",
    "redis>=5.0.0",
    "resend>=0.14.0",
    "email-validator>=2.1.0",
    "python-multipart>=0.0.9",
    "pypdf>=4.0.0",
    "pgvector>=0.3.0",
    "reportlab>=4.2.0",
    "openai>=1.40.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "httpx>=0.27.0",
    "ruff>=0.5.0",
    "mypy>=1.10.0",
    "types-reportlab>=4.2.0",
    # API tests exercise worker tasks directly (e.g. send_reminder_emails),
    # so the worker's runtime deps must be present in the API dev env.
    "celery>=5.4.0",
]

[tool.uv]
package = false

[tool.ruff]
line-length = 100
target-version = "py312"

exclude = [
    "alembic/versions",
]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B"]
ignore = ["E501", "B008"]

[tool.mypy]
python_version = "3.12"
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
