import { EmptyState } from '../../../components/ui/EmptyState'

export function IntegrationsPage() {
  return (
    <EmptyState
      title="Integrations"
      description="The email digest, LinkedIn capture, and calendar reminders live here — they arrive with the retention and expansion phases."
    />
  )
}